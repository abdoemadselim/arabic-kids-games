type GameType = {
  id: number;
  name: string;
  thumbnail: string;
  trailer: string;
  full_image: string;
  url: string
  image: string
};

type FileType = {
  publicUrl: string;
};


type CategoryType = {
  name: string,
  icon: string
}